// The package publishes these shapes; this file keeps the import path the
// handlers use.
export type { EosioAction, EosioActionTrace, EosioContractRow, EosioTransaction } from '@atomichub/antelope-ship-utils';
