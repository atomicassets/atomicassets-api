# Registry/network settings only - pnpm's own config lives in pnpm-workspace.yaml.

# Network resilience for CI/Docker builds.
fetch-retries=3
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=60000
fetch-timeout=120000
